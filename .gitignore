# Stray scratch file from an unrelated project (LangSlice) — do not track
tmp_rp.txt

# --- AI-generated docs: kept locally, not published to the public repo ---
# Spec/plan output from the superpowers workflow
/docs/superpowers/
# Ad-hoc research / reverse-engineering write-ups (matches at any depth)
*_RESEARCH.md
# One-off working docs not meant for the public repo
/docs/release-readiness.md
/docs/transport-decisions.md
/docs/ACP_TRANSPORT_BLUEPRINT.md

# --- Tooling ---
node_modules/

# Agent scratch area: plans, specs, reverse-engineering notes. Local-only.
/.agent/

# Local promo tooling, not part of the repository
/docs/assets/promo/
/scripts/render-promo.mjs

# Node diagnostic reports written by test/watchdog.ts
report.*.json

# Local Claude agent worktrees are independent checkouts, not source inputs.
/.claude/worktrees/

# Claude Code function-hook declarations, written per build by `/plugin-types`;
# `npm run typecheck:mod` reads them. Regenerate after an update, never edit.
/.claude/types/
